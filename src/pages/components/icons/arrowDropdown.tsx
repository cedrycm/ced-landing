import React from 'react'
import { IconBaseProps } from "react-icons/lib";

interface arrowDropdownProps extends IconBaseProps {
  isOpen: boolean;
}

export const arrowDropdown: React.FC<arrowDropdownProps>  = ({}) => {
  return (
    
    );
}